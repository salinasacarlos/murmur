/**
 * URL pública canónica de Murmur en producción (sin barra final).
 * Usar como fallback cuando falte NEXT_PUBLIC_SITE_URL (p. ej. Stripe success_url).
 */
export const CANONICAL_SITE_ORIGIN = "https://joinmurmur.xyz" as const
